import styled from 'styled-components';

interface Props {}

function BooksViewSwitcher(props: Props) {
  return (
    <BooksViewSwitcherStyle>
      <h1>BooksViewSwitcher</h1>
    </BooksViewSwitcherStyle>
  );
}

const BooksViewSwitcherStyle = styled.div``;

export default BooksViewSwitcher;
